# User Frontend — Prototype

This folder contains a lightweight static html/css folder that conta.

Notes
- This folder is intentionally minimal.

- The html file may request data from the backend; start the backend before opening the page if you want live data. Example (from repo root):

```powershell
cd backend
npm install
copy .env.example .env
npm run dev
```

Open ```http://localhost:3001``` for the html index file for testing. 

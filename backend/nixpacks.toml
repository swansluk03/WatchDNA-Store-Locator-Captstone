[phases.setup]
nixPkgs = ["nodejs_20", "python312"]

[phases.install]
cmds = [
  "npm ci",
  "python3 -m venv /opt/pyenv",
  "/opt/pyenv/bin/pip install requests phonenumbers playwright geopy"
]

[variables]
PYTHON_PATH = "/opt/pyenv/bin/python3"

[start]
cmd = "npm start"
